@macro test_symbols {
  @debug "_0 = " _0
  // => #{_0} = Symbol(_0)
  @debug "#{_0} = " #{_0}
  // => #{_0} = Symbol(my_symbol)
  @debug "#{#{_0}} = " #{#{_0}}
  // => "#{#{_0}} = " hello
}

@set my_symbol hello
@test_symbols my_symbol
